import { describe, it} from 'mocha';
import { should } from 'chai';

describe( 'BaseEmitter', function() {
    describe( 'serializeState()', function() {
        it( 'Should allow plain text serialization', function() {
            should().fail("Not implemented");
        });
        it( 'Should allow aes gcm serialization', function() {
            should().fail("Not implemented");
        });
        it( 'should be compatible with recreateEmitter in provider', function() {
            should().fail("Not implemented");
        });
    });
});