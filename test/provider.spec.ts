import { describe, it} from 'mocha';
import { should } from 'chai';

describe( 'Provider', function() {
    describe( 'registerEmitterFactory()', function() {
        it( 'Should result in factory calls when using that type', function() {
            should().fail();
        });
    });
    describe( 'registerChroniclerFactory()', function() {
        it( 'Should result in factory calls when using that type', function() {
            should().fail();
        });
    });
    describe( 'recreateEmitter()', function() {
        it( 'Should create a previous emitter with the same state', function() {
            should().fail();
        });
    });
    describe( 'buildEmitter()', function() {
        it( 'Should build an emitter to expected specifications', function() {
            should().fail();
        });
    });
    describe( 'buildChronicler()', function() {
        it( 'Should build an chronicler to expected specifications', function() {
            should().fail();
        });
    });
});